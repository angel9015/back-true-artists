<% set_meta_tags title: "Tattoo Ideas, Guides & Advice",
                 description: "Your go to blog for Tattoo Ideas, Guides & Advice. Don't miss out on great content around tattoo lifestyle & culture" %>

<div class="page-section gray-bg">
  <div class="container">
    <div class="row">
      <div class="col-md-12 search-form">
        <%= form_for articles_path, html: { method: :get } do |f| %>
          <div class="sub-header">
            <h1>Articles</h1>
          </div>
          <div class="search-artists-form row">
            <div class="col-md-12">
              <div class="search-section">
                <%= text_field_tag "query", params[:query], class: '', placeholder: 'Search' %>
                <button type="submit" class="search search-box">
                  <i class="ta-icon-search basic-search"></i>
                </button>
              </div>
            </div>
          </div>
          <% if params[:query].present? %>
            <div class="selected-filters">
              <p>Showing search results for <b><%= params[:query] %></b></p>
            </div>
          <% end %>
        <% end %>
      </div>
    </div>
  </div>
</div>

<div class="page-section">
  <div class="container">
    <div class="articles-lists row">
      <%= render @articles %>
    </div>
  </div>
</div>
