<div class="col-lg-3 col-md-6 grid-item image-item">
  <%= link_to article_path(article), class: 'gallery-item' do %>
    <img data-src="<%= url_for(article.image) %>" class="lazyload" />
  <% end %>
  <h5><%= url_for(article.title) %></h5>
</div>
